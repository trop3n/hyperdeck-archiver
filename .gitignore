# Probe artifacts
logs/
*.log

# Secrets
.env
.env.*
!.env.example

# Local config (site-specific; copy from config.example.yaml)
config.yaml

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Tool caches (pytest/ruff self-ignore via inner .gitignore; explicit is robust)
.pytest_cache/
.ruff_cache/

# Transient session notes (not part of the shipped project)
HANDOFF.md

# OS
.DS_Store
