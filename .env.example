# Secrets - copy to .env and fill in. NEVER commit .env.
SMTP_USER=archiver@example.com
SMTP_PASS=your-app-password
SMTP_FROM=archiver@example.com
# Only needed if you use the optional auto-mount helper:
NAS_USER=archiver
NAS_PASS=your-nas-password
